import { minify } from 'uglify-js';
import buble from 'rollup-plugin-buble';
import uglify from 'rollup-plugin-uglify';
import { Banner, outputConfig } from './pack.banner';

export default {
	entry: './src/main/index.js',
	dest: './dist/geisha.min.js',
	format: 'umd',
	moduleName: 'Geisha',
	banner: Banner,
	plugins: [
		buble(),
		uglify({
			output: outputConfig
		}, minify)
	]
}
